/**
 * Create a pretty report from files.
 *
 * @param {Array<VFile>} vfiles
 *   Files to report.
 * @returns {string}
 *   Pretty report.
 */
export function reporterPretty(vfiles: Array<VFile>): string
export type VFile = import('vfile').VFile
